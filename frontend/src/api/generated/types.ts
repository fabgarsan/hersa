// Stable import surface for OpenAPI-generated types.
//
// ALWAYS import from this file — never directly from "./schema".
// Indirection here insulates consumers from schema churn (regeneration).
//
// PREREQUISITE: schema.ts is generated by `npm run generate:types` (git-ignored).
// Run it after any backend change, and always before `tsc` in CI:
//
//   npm run generate:types   # from frontend/
//
// Once the schema exists, all types declared in schema.ts are re-exported below.
// Until then, TypeScript will error on this import — run generate:types first.

// eslint-disable-next-line @typescript-eslint/ban-ts-comment
// @ts-ignore — schema.ts is generated; run `npm run generate:types` if missing
export type * from "./schema";
